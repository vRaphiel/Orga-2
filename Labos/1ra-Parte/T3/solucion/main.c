#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
#include <assert.h>

int main(void)
{
	/* Acá pueden realizar sus propias pruebas */
	// assert(alternate_sum_4(8, 2, 5, 1) == 10);
	// assert(alternate_sum_4_using_c(8, 2, 5, 1) == 10);
	// assert(alternate_sum_4_simplified(8, 2, 5, 1) == 10);

	/*/**
	 * Las discrepancias en checkpoint 2 y 3 se deben a las funciones de producto con punto flotante
	 * Nos dan variaciones de presición en los numeros y varia en unas pocas cifras.
	 */
	return 0;
}
